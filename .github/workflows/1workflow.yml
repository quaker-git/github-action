name: 1workflow
on: push
jobs:
  work_1flow:
    name: first-workflow
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: npm lint execution
        run: npm install lint